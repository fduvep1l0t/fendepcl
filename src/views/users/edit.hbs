<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card ">
                <div class="card-body">
                    <form action="/users/edit/{{user.id_user}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{user.user_acc}}" class="form-control" name="user_acc" placeholder="Nombre de cuenta" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{user.user_pw}} "name="user_pw" class="form-control" placeholder="Contraseña">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Cambiar info
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>